import React from "react";
function Title({title}){
    return(
        <h1>{title}</h1>
    );
};
// function Title(props){
//     return(
//         <h1>{props.title}</h1>                we can do both things
//     );
// };
export default Title;